/**
 * Global variables for Shamaze3D.
 * These are shared across multiple modules.
 */

var camera,
    scene,
    renderer,
    light,
    mouseX,
    mouseY,
    maze,
    mazeMesh,
    mazeDimension = 11,
    planeMesh,
    ballMesh,
    ballRadius = 0.25,
    gameState;
